using UnityEngine;

namespace Refractions
{
    public class RefractionBlockDef : MonoBehaviour
    {
        public float ior = 1.5f;
        
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
